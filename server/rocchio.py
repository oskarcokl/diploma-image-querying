def make_new_query(query_features, related_images: dict, not_related_images: dict, query_weight=1, related_documents_weight=0.8, non_related_documents_weight=0.1):
    print(query_features)
    return
