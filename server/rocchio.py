def make_new_query(query_features, related_images, non_related_images, query_weight=1, related_documents_weight=0.8, non_related_documents_weight=0.1):
    print(query_features)
    print(len(related_images))
    print(len(non_related_images))
    return
